# HAN-FIGHT Project Rules

이 문서는 새 Codex 세션에서 반드시 먼저 읽고 따라야 하는 작업 규칙이다.
지금까지의 작업에서 반복적으로 문제가 되었던 지점을 기준으로 정리했다.

## 기본 원칙

- 사용자는 한국어로 작업한다. 답변과 게임 내 신규 텍스트는 한국어를 우선한다.
- 기존 한글 문구를 임의로 번역하거나 영문으로 바꾸지 않는다.
- 한글이 깨질 수 있으므로 대량 치환, 인코딩 변경, 전체 파일 재작성은 매우 조심한다.
- `index.html`은 HTML만 유지한다. CSS는 `style.css`, JS는 `game.js`에 둔다.
- `game.js` 수정 후 항상 문법 검사를 한다.
  - `node -e "const fs=require('fs'); const js=fs.readFileSync('game.js','utf8'); new Function(js); console.log('game.js parse ok');"`
- 코드 변경은 기능 단위로 작게 한다. unrelated refactor 금지.

## 파일 구조

- 메인 HTML: `index.html`
- 메인 JS: `game.js`
- 메인 CSS: `style.css`
- 에셋: `assets/`
- 서비스워커: `sw.js`

`index.html`에 다시 큰 `<style>` 또는 `<script>` 블록을 넣지 않는다.

## 에셋 생성 규칙

- 도트/스프라이트 에셋은 게임 톤에 맞게 픽셀아트로 만든다.
- 생성 이미지는 반드시 프로젝트 `assets/`에 저장하고 코드에서 그 경로를 참조한다.
- 투명 배경이 필요한 이미지는 #00ff00 같은 크로마키 배경으로 생성한 뒤 로컬에서 제거한다.
- 이미지 시트는 코드에서 자를 규격을 명확히 맞춘다.
- 프레임 경계에 캐릭터나 이펙트가 닿지 않게 충분한 패딩을 둔다.
- 스프라이트가 잘리는 문제를 막기 위해 프레임별 캐릭터 크기, 발 위치, 중심축을 통일한다.
- 단순히 좌우 반전하면 안 되는 캐릭터나 자전거/도구 방향은 별도 방향 프레임을 만든다.

## 스프라이트 규격

- 보스 4방향 이동 스프라이트 순서:
  - 1행: 정면
  - 2행: 왼쪽
  - 3행: 오른쪽
  - 4행: 뒤
- 등장/죽음/스킬 모션은 이동 모션과 캐릭터 크기 및 기준점을 맞춘다.
- 캐릭터 선택/컷신/인게임에서 같은 캐릭터가 갑자기 커지거나 작아지면 안 된다.
- 이동 모션과 공격 모션을 겹쳐 사용할 때는 기준점이 흔들리지 않는지 확인한다.
- 공격 모션이 이동 모션과 충돌하면, 별도 공격 모션만 사용하거나 자연스럽게 통합한다.

## 장판/광역 이펙트 규칙

- 장판형 스킬은 CSS 느낌의 단순 원, 선, 회전 도형만으로 만들지 않는다.
- 바닥에 깔리는 장판은 탑다운 시점에 맞춰 살짝 눌린 원형/타원형 느낌을 준다.
- 너무 선명한 이미지 시트를 크게 확대하면 깨져 보이므로, 큰 장판은 캔버스 그라디언트와 작은 픽셀 입자를 섞는 편이 낫다.
- 경고 장판은 붉은 계열로 통일한다.
- 실제 공격 이펙트와 예상 장판은 구분되어야 한다.
- 화면 밖에 있는 적을 공격하거나, 화면 밖으로 스킬이 발사되지 않게 한다.

## 보호막/쉴드 규칙

- 회전 방패와 보호막은 다른 스킬이다.
- 회전 방패:
  - 레벨별 개수 증가
  - 원거리 공격을 막으면 방패 하나 소모
  - 몬스터와 부딪히면 몬스터에게 피해를 주고 방패 하나 소모
  - 시간이 지나면 하나씩 재생
  - 진화 시 개수/데미지/재생 속도 증가
- 보호막:
  - 피해를 대신 받아주는 체력형 방어막
  - 스킬을 찍는 즉시 보호막이 생성되어야 한다.
  - 깨진 뒤에는 일정 시간이 지나야 재생된다.
  - 비주얼은 단순 원형보다 SF 보호막 느낌이 좋다.
  - 캐릭터가 보이는 중앙은 과한 패턴으로 가리지 않는다.
  - 육각 셀을 쓸 경우 중앙에 균일하게 깔지 말고 외곽 표면에만 자연스럽게 나타나게 한다.

## 스킬 시스템 규칙

- 액티브 스킬은 최대 3종 획득 가능.
- 패시브 스킬은 최대 4종 획득 가능.
- 각 스킬은 5레벨까지 성장하고, 5레벨 이후 진화 선택지가 뜬다.
- 플레이어 기본공격도 같은 방식으로 5레벨 후 진화한다.
- 화염용과 얼음새는 동시에 획득할 수 없다.
- 테스트모드 레벨업 버튼은 모든 스킬 카드를 작게 표시해서 원하는 스킬을 선택할 수 있어야 한다.
- 일반 플레이 레벨업은 랜덤 3장 유지.

## 플레이어블 캐릭터 규칙

- 캐릭터 선택창에서 잠긴 캐릭터 카드는 볼 수 있지만 시작은 불가능해야 한다.
- 잠기지 않은 캐릭터가 먼저 정렬된다.
- 보스 컷신에는 선택한 플레이어블 캐릭터가 나와야 한다.
- 필살기 멘트는 캐릭터별 데이터로 관리하고, `isSangil` 같은 개별 플래그 분기를 계속 늘리지 않는다.
- 필살기 중이라고 완전 무적이면 안 된다. 맞으면 피해를 받을 수 있어야 한다.

## 상일 규칙

- 상일은 근접 캐릭터.
- 기본 공격은 가까운 적 방향으로 검기/클로가 나가야 한다.
- 5레벨 이후 진화해야 강화 공격이 적용된다.
- 휠윈드는 이동 가능하고, 지속시간 종료 후부터 쿨타임이 돌아야 한다.
- 휠윈드 멘트: `상일, 가고 싶은대로 간다~!!`

## 승관 규칙

- 승관은 철공 포환을 포물선으로 던진다.
- 철공은 원형으로 보여야 하며 타원형으로 찌그러지면 안 된다.
- 필살기 멘트: `정훈아~ 가즈아~~~!!`
- 필살기는 현재 제자리 점프 기반으로 처리한다.
- 필살기 중 기본공격은 멈춘다.

## 스테이지/난이도 규칙

- 맵은 랜덤으로 등장하지만 난이도는 맵 종류가 아니라 몇 번째 스테이지인지에 따라 정한다.
- 1스테이지는 쉽고, 스테이지가 올라갈수록 몬스터 체력/수/속도/보스 강도가 올라간다.
- 몬스터 젠은 무작위 난사보다 패턴이 있어야 한다.
- 초반에는 적게 나오고 시간이 지나며 점진적으로 늘어난다.
- 웨이브 타임은 정해진 수의 몬스터가 패턴으로 몰려오고, 어느 정도 정리된 뒤 보스전으로 넘어간다.
- 보스전 진입 시 바닥 아이템을 먼저 청소한 뒤 소주를 제공한다.
- 보스 클리어 시 소주 2병을 제공한다.

## 보스전 규칙

- 보스는 스테이지 스토리에 묶인다. 보스만 랜덤으로 섞지 않는다.
- 스테이지가 뒤로 갈수록 같은 보스라도 강해져야 한다.
- 보스 컷신, 등장, 사망, 클리어 문구는 보스별로 맞아야 한다.
- 1보스, 2보스, 3보스 등 공통으로 쓸 수 있는 컷신/울타리/체력바 로직은 하드코딩하지 말고 공유한다.
- 보스전 울타리 생성은 갑자기 시작하지 않고 자연스럽게 전개한다.

## 모바일 규칙

- 모바일에서는 테스트 버튼을 숨긴다.
- 모바일 조작은 멀티터치를 지원해야 한다.
- 모바일 UI는 화면을 과하게 가리지 않는다.
- 모바일에서는 카메라를 조금 더 멀리 보이게 한다.
- 원거리 공격은 화면 밖에서 갑자기 날아오는 느낌이 나면 안 된다.
- 보스 컷신과 스테이지/클리어 텍스트는 화면 크기에 맞춰 비율 조정한다.

## HUD/UI 규칙

- HUD는 PC 고정 게임 해상도 내부에 배치한다.
- 모바일/PC 모두 스킬 HUD의 레벨 숫자는 눈에 잘 보여야 한다.
- 스킬 HUD는 아이콘과 레벨 중심으로 간결하게 표시한다.
- 미니맵은 삭제한 상태를 유지한다.
- 토스트 메시지는 사용하지 않는다.
- 스테이지 시작 문구는 `survive` 대신 맵 이름을 표시한다.

## BGM/SFX 규칙

- 맵별 일반 BGM과 보스 BGM을 구분한다.
- 보스 클리어 모션 중에는 보스 BGM을 유지하고, 다음 스테이지 전환 시 일반 BGM으로 바꾼다.
- 일시정지나 레벨업 선택 화면 때문에 BGM이 처음부터 다시 시작되면 안 된다.
- 일시정지 메뉴에서 BGM/SFX 음량을 0~10 단계로 조절할 수 있어야 한다.
- BGM이 효과음을 묻지 않게 볼륨 밸런스를 조정한다.

## 성능/렉 규칙

- 큰 단일 파일은 피한다. 이미 `index.html`, `style.css`, `game.js`로 분리했다.
- 화면 밖 오브젝트는 가능한 한 드로우하지 않는다.
- 너무 많은 이펙트/보석/아이템/몬스터가 누적되지 않도록 상한을 둔다.
- 큰 이미지 에셋을 매 프레임 과하게 확대해서 그리지 않는다.
- 새 에셋 추가 후 로딩/캐시 영향도 고려한다.

## 검증 체크리스트

작업 후 최소한 다음을 확인한다.

- `game.js parse ok`
- 새 이미지가 `assets/`에 존재하는지
- 코드에서 참조하는 파일명이 실제 파일명과 일치하는지
- 한글 문구가 깨지지 않았는지
- 테스트모드 버튼으로 해당 스테이지/보스/스킬을 빠르게 확인 가능한지
- 모바일 UI가 과하게 겹치지 않는지
- 기존 유저가 만든 변경사항을 되돌리지 않았는지


## 테스트 모드/캐시 규칙

- 테스트 도구는 플레이 화면 HUD에 고정하지 않고 `ESC` 일시정지 메뉴에서만 연다.
- 테스트 선택 카드는 `stageThemes` 데이터에서 자동 생성한다.
- 새 스테이지와 보스는 `stageThemes`에 등록하면 테스트 선택창에도 자동으로 나타나야 한다.
- 테스트 레벨업은 모든 스킬 카드를 표시하고, 무적 토글도 ESC 테스트 메뉴에 둔다.
- 캐시 무효화 버전 쿼리는 `game.js`와 `style.css`에만 사용한다.
- 이미지와 음원에는 버전 쿼리를 붙이지 않고 서비스워커의 캐시 우선 정책을 사용한다.
## 플레이어블 캐릭터 선택창/스프라이트 추가 규칙

- 캐릭터 선택창의 `trait`는 성격이나 임의 설정이 아니라 실제 플레이 특징만 적는다.
- 새 플레이어블을 만들 때 기존 선택창 초상화와 기존 설명문은 사용자가 명시하지 않으면 변경하지 않는다.
- 4방향 이동 스프라이트 행 순서는 반드시 `0: 정면/down`, `1: 왼쪽/left`, `2: 오른쪽/right`, `3: 후면/up`으로 맞춘다.
- 각 셀의 캐릭터는 머리, 발, 장비가 셀 경계에 닿지 않도록 충분히 패딩한다.
- 생성형 이미지 시트가 셀 경계에서 잘리거나 좌우가 뒤집히면 그대로 쓰지 말고 리팩하거나 코드/픽셀 기반 안정 시트로 교체한다.